{
  "name": "cloudflare-workers",
  "version": "1.0.0",
  "description": "FCM Web Push notifications with Hono on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["hono", "cloudflare-workers", "fcm", "web-push"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.6.14",
    "wrangler": "^3.88.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@cloudflare/workers-types": "^4.20241120.0"
  }
}
